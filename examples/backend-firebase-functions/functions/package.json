{
  "name": "pwa-push-backend-firebase-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Reference Firebase Functions v2 + Firestore push backend for the Hugo PWA module.",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "vapid": "npx web-push generate-vapid-keys"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "web-push": "^3.6.7"
  }
}
