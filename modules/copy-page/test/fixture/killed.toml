# Overlay config for the site-wide kill-switch build. Merged AFTER hugo.toml
# via --config hugo.toml,killed.toml, so only this key differs from the
# served fixture: the master switch alone must strip every widget root and
# every script tag from the whole build.
[params.copy_page]
enable = false
