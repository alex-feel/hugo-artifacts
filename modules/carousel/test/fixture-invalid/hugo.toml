baseURL = "http://localhost:1717/"
defaultContentLanguage = "en"
title = "Carousel Fixture Invalid"

[languages]

  [languages.en]
    locale = "en-US"
    weight = 1

[params]
description = "Negative-build fixture: a single page calling the carousel shortcode with both match and items, which must errorf and fail the build."

[module]

  [module.hugoVersion]
    min = "0.160.0"

  [[module.imports]]
    path = "github.com/alex-feel/hugo-artifacts/modules/carousel"
