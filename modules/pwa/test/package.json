{
  "name": "hugo-pwa-validation-matrix",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright validation matrix for the Hugo PWA module (9-row coverage of SW, manifest, RFG, install, push, offline, update, Lighthouse).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:offline-gating": "node scripts/offline-gating-check.js",
    "rfg-check": "node scripts/rfg-checker.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
