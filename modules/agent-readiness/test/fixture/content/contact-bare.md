---
title: 'Contact page with bare channel entries'
description: 'Its channels are bare strings inside a real slice, not tables.'
# The bare-string-array shape for a contact page's channels. Dropped silently
# the channel simply disappears from the contact block with no signal.
channels: ['email', 'repository']
---

Read only by the badtables build.
