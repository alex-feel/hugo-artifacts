---
title: 'Duplicate Permalink Probe'
description: 'A section whose two pages deliberately publish to one URL.'
---

Admitted to the agent surfaces only by the `edge` environment's allow-list, so the collision guard is reachable without disturbing any other build's counts.
