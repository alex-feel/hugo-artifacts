---
name: some-other-name
description: A fixture skill whose front-matter name disagrees with the name it is configured and published under.
---

# Fixture mismatch

The specification requires the front-matter name to match the parent directory, and the parent directory is the configured name, so these two must agree.
