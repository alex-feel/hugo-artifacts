---
name: fixture-single
description: A single-file fixture skill, used to prove the fetch, republish and digest chain end to end. Its body names no sibling file of any kind, so the supporting-file detector must stay silent about it.
---

# Fixture single

This skill exists to be published. Everything it has to say fits in one file, which is exactly the shape the discovery convention distributes as a single SKILL.md.

The body deliberately avoids every shape the detector nominates: no relative link, no slash-bearing path, no capitalised file name. A warning about this entry is therefore a defect in the detector rather than a fact about the skill.
