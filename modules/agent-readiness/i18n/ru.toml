# Russian UI strings for the agent-readiness module. Templates never call
# T directly: agent-readiness/config.html resolves every key once into
# $cfg.i18n through the repository's English-default idiom
# ({{ T "key" | default "English" }}), so a site whose language ships no
# translation still renders a real heading rather than an empty string.
#
# The module authors exactly four strings, all of them headings in generated
# documents. "## Optional" in llms.txt is deliberately NOT a key here: it is
# fixed by the llmstxt.org convention and is a protocol token, not prose.
#
# This file carries flat keys only. Should a plural table ever be added, it
# MUST sit LAST: a TOML table header claims every key-value pair that follows
# it until the next header, so a mid-file table would silently swallow all the
# flat keys below it.

# Heading of the trailing pointer section in a Markdown twin and in about.md.
agent_sitemap_heading = "Карта сайта"

# H1 fallback for about.md when [params.agent.facts] title is unset and the
# site has no title.
agent_facts_title = "О себе"

# H2 of the identity block in about.md.
agent_facts_identity_heading = "Идентификация"

# H2 of the contact block in about.md.
agent_facts_contact_heading = "Контакты"
