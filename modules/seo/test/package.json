{
  "name": "seo-module-tests",
  "private": true,
  "type": "module",
  "description": "Build-output assertion suite for the seo Hugo module, run against the static HTML that nine hugo builds of ./fixture produce -- the baseline, configured, subpath, badtypes, offswitch, multilingual, pagination, graph and sitename environments. The module ships zero JavaScript, so there is no browser behavior to test.",
  "scripts": {
    "test": "node --test \"tests/*.spec.js\""
  },
  "devDependencies": {
    "node-html-parser": "^7.0.1"
  }
}
