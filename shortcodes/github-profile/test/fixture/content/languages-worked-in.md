---
title: 'Languages, worked-in scope'
description: 'The language row under language-scope="worked-in".'
---

A second page rather than a second call on the home page, so the two configurations never share a document. Every existing spec addresses the home page by name and keeps seeing exactly one widget, one language row, one rank element and one pair of streak wrappers; this page adds the opt-in scope without putting a second instance of any of them in their way.

Only the `languages` section renders here, because that is the only section `language-scope` reaches.

{{< github-profile user="fixture-dev" sections="languages" language-scope="worked-in" >}}
