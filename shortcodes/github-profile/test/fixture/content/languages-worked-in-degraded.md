---
title: 'Languages, worked-in scope, authorship degraded'
description: 'The worked-in language row after a failed authorship request.'
github_profile_fixture_authorship_fail: true
---

The front-matter flag makes the fixture seam report `authorshipOk false` with an empty map -- the exact shape the module's fetch partial returns after its authorship retries are exhausted -- so this page publishes the row `derive.html` computes from its fallback numerators, the contribution-window commit counts. A third page rather than a second call on the worked-in page, for the same reason that page is not a second call on the home page: every spec keeps seeing exactly one widget per document.

{{< github-profile user="fixture-dev" sections="languages" language-scope="worked-in" >}}
