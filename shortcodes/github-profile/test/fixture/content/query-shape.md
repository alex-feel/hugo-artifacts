---
title: 'GraphQL request shape'
description: 'Publishes the output of the module build-query.html partial so a spec can assert it.'
layout: 'query-shape'
---

This page renders no widget. Its layout calls the module's query builder directly and publishes what it returns, because the fixture's offline seam replaces that partial's only caller and nothing else in this repository ever executes it.
